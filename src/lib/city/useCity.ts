/**
 * useCity — convenience re-export.
 * Source of truth is CityContext.
 * Import from here or from src/context/CityContext directly.
 */
export { useCity, CityProvider } from '../../context/CityContext';
export type { SpiceKreweCity } from '../../context/CityContext';
